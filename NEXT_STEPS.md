# NEXT_STEPS.md

- Current milestone: Demo Payment Confirmation Milestone
- Completed capabilities: Mock intelligence endpoints, AIS abstraction, commerce/x402 stubs, official `x402-avm` middleware protection for ETA risk, centralized dotenv-backed config, committed `.env.example`, OpenAPI 402 docs, tests for disabled, unpaid, paid-mocked, unprotected routes, and the new demo-payment endpoint, a dedicated live demo runbook in `docs/TESTNET_DEMO.md`, and a thin Hamburg Cargo frontend in `frontend/` that distinguishes real `402` responses, real backend-confirmed payment attempts, manual settlement evidence, whitelist-based auto-pay policy, and demo-only intelligence preview states.
- Immediate next recommendation: Fund the backend demo payer with TestNet ALGO and TestNet USDC, click the frontend Confirm x402 Payment button against the protected ETA risk route, and verify the resulting transaction evidence in Lora.
- Restart guidance: Start with `app/features/commerce/demo_payment_service.py` for the backend demo payer flow, then review `frontend/src/App.tsx` for manual confirm and whitelist auto-pay states, and finally use `docs/TESTNET_DEMO.md` to fund the payer and receiver accounts before attempting a live paid browser run.
- Future roadmap: Validate the live browser confirm-payment flow, replace demo-only preview reliance with real paid release evidence wherever possible, decide whether to keep TestNet USDC or move to a Quantoz-backed flow later, extend protection to additional intelligence products only after the ETA risk path is stable, and then add real AIS ingestion, persistence, and customer-facing access controls.
