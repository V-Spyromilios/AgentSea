# NEXT_STEPS.md

- Current milestone: Multi-Product x402 Demo Milestone
- Completed capabilities: Mock intelligence endpoints, AIS abstraction, commerce/x402 stubs, official `x402-avm` middleware protection for ETA risk and port congestion, centralized dotenv-backed config, committed `.env.example`, OpenAPI 402 docs, tests for disabled, unpaid, paid-mocked, and protected routes, demo-payment endpoints for ETA risk and port congestion, a dedicated live demo runbook in `docs/TESTNET_DEMO.md`, a thin Hamburg Cargo frontend in `frontend/` that distinguishes real `402` responses, real backend-confirmed payment attempts, whitelist-based auto-pay policy for ETA risk, a manual-confirm independent port congestion purchase flow, demo-only intelligence preview states, a human-approved warehouse email draft flow built on top of paid ETA intelligence, and a deterministic exporter-message extraction step that populates the structured supplier claim before payment.
- Immediate next recommendation: Freeze features, run the full live demo, capture Lora transaction evidence for ETA risk and port congestion, and polish the presentation.
- Restart guidance: Start with `app/features/commerce/` for the shared x402 middleware and demo payer flow, then review `app/features/port_congestion/` for the second protected product, continue with `frontend/src/App.tsx` for the independent port congestion UI state, and finally use `docs/TESTNET_DEMO.md` before attempting a live paid browser run.
- Future roadmap: Validate the full browser flow from message extraction to paid release, replace demo-only preview reliance with real paid release evidence wherever possible, decide whether to add optional structured LLM extraction behind the current service boundary, add real provider adapters for port congestion only after the multi-product paywall is stable, and then add real AIS ingestion, persistence, customer-facing access controls, and any future outbound action integrations with explicit approval and audit boundaries.
