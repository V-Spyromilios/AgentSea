# NEXT_STEPS.md

- Current milestone: Supplier Claim Extraction Demo Milestone
- Completed capabilities: Mock intelligence endpoints, AIS abstraction, commerce/x402 stubs, official `x402-avm` middleware protection for ETA risk, centralized dotenv-backed config, committed `.env.example`, OpenAPI 402 docs, tests for disabled, unpaid, paid-mocked, unprotected routes, the demo-payment endpoint, a dedicated live demo runbook in `docs/TESTNET_DEMO.md`, a thin Hamburg Cargo frontend in `frontend/` that distinguishes real `402` responses, real backend-confirmed payment attempts, manual settlement evidence, whitelist-based auto-pay policy, demo-only intelligence preview states, a human-approved warehouse email draft flow built on top of paid ETA intelligence, and a deterministic exporter-message extraction step that populates the structured supplier claim before payment.
- Immediate next recommendation: Edit the exporter message in the frontend, verify that the extracted promised ETA changes the protected ETA-risk request URL, and then run the real TestNet confirm-payment path through to paid intelligence and the warehouse draft.
- Restart guidance: Start with `app/features/message_extraction/` for deterministic supplier claim parsing, then review `frontend/src/App.tsx` for the exporter-message card and reset behavior, continue with `app/features/commerce/demo_payment_service.py` for the backend demo payer flow, and finally use `docs/TESTNET_DEMO.md` before attempting a live paid browser run.
- Future roadmap: Validate the full browser flow from message extraction to paid release, replace demo-only preview reliance with real paid release evidence wherever possible, decide whether to add optional structured LLM extraction behind the current service boundary, extend protection to additional intelligence products only after the ETA risk path is stable, and then add real AIS ingestion, persistence, customer-facing access controls, and any future outbound action integrations with explicit approval and audit boundaries.
